---
"agent-skills-ts-sdk": minor
---

Add syntax-only document parsing with inferred host schema output, extension field policies, and composable validators shared by content, file-entry, and patch validation. Expose registry catalog data for custom presentation while keeping existing prompt helpers and strict validation defaults.

Align resource links and the reference lock with the current AgentSkills specification, preserve prototype-like metadata keys and literal delimiter text in scalar values, and avoid duplicate parsing during patch name validation. Enforce coverage in CI and add reproducible benchmarks.
